package application;

import javafx.scene.paint.Color;
import javafx.scene.shape.Rectangle;

public class Obstacles  {
	public void Obstacles(int level) {
    Rectangle rect = new Rectangle();
    rect.setHeight(10);
    rect.setWidth(10);
    rect.setFill(Color.BLACK);
    }

}
